# Native latest candidate failure: 2.1.150

- Run: https://github.com/taekchef/claude-code-zh-cn/actions/runs/26325594573
- Head SHA: `bfff1b30470f8c94e697e5cfde8598c1553f1332`
- Status: `fail`
- Candidate kind: `native`
- Native: `darwin-arm64` / `ok` / `ok` / `ok`
- Display audit: `fail` (1 issues / 11 commands)

## What Failed

- Candidate verification did not pass: `status=fail`.
- Display audit did not pass: `status=fail`.
- Summary: pass=0, fail=1, skip=0.

## Display Audit Issues

- `top_help_line_25` (`top_help`, display-untranslated-line)

  ```text
  --effort <level>                                  Effort level for the current session (low, medium, high, xhigh, max)
  ```

## Text Diff Excerpt

```markdown
# Upstream text diff: 2.1.148 -> 2.1.150

- Added upstream strings: 4498
- Removed upstream strings: 6685
- Already covered by translations: 23
- Needs translation review: 4475
- Sensitive review hints: 48

## Added strings needing review
- _valid
- - e.g.
- ----FormBoundary${...}
- --agent-color ${...}
- --agent-id ${...}
- --agent-name ${...}
- --agent-type ${...}
- --model ${...}
- --parent-session-id ${...}
- --permission-mode acceptEdits
- --permission-mode auto
- --plugin-dir ${...}
- --plugin-url ${...}
- --settings ${...}
- --team-name ${...}
- , // meta: { chat_id:
- , // params: { // content:
- , baseURL="${...}"
- , description:
- , inputSchema: { type:
- , matches: ${q.join(", ")} `),process.exit(1);let K=q[0],O=await c7(b4(K)),{removed:T,error:z,keptWorktree:$,keptReason:A}=await a8H(K);if(!T)uH("cli_bg_rm","kill_unconfirmed"),process.stderr.write(`couldn
- , or an integer."),shell:S2().optional().describe("Shell for
- , properties: { text: { type:
- , sender:
- , text:
- ,__.circle,
- ,__.tick,
- ,__.tick))})),z5.default.createElement(B,{flexDirection:
- ,__.warning,
- ,_,q),debug:UE_(
- ,_,q),info:UE_(
- ,_,q),warn:UE_(
- ,_.identity.agentName),H[3]=K,H[4]=_.identity.agentName,H[5]=T;else T=H[5];let z;if(H[6]===Symbol.for(
- ,_.nested.google.nested.protobuf.nested),fiH.common(
- ,_.querySource);let w=UJ();if(w?.agentId)Y.setAttribute(
- ,_),K=await fetch(q,{...Yj({url:String(q)}),headers:{Accept:
- ,_)(`${K.connected} connected`));if(K.needsAuth)O.push(Eq(
- ,_)(`${K.disabled} disabled`));if(K.failed)O.push(Eq(
- ,_)(`${K.needsAuth} need auth`));if(K.pending)O.push(Eq(
- ,_)(`${K.pending} pending`));if(K.disabled)O.push(Eq(
- ... 4435 more

## Added strings already covered
- All background agents stopped
- Allowed by auto mode classifier
- Background process cannot read terminal input
- Background process cannot write to terminal output
- Backgrounded agent
- Command should be emulated but is not implemented
- Floating point arithmetic error
- for history)
- hook error
- hook returned blocking error
- hook stopped continuation:
- hook warning
- Listed directory
- PR #
- Referenced file
- Referenced PDF
- Remote agent launched
- Request for process information
- Searched for
- Searching for
- Tool use rejected
- User interruption with CTRL-BREAK
- User interruption with CTRL-C

## Removed strings
- __autonomous_preamble__
- __other__
- _proxy
- - **id**: Task identifier (use with TaskGet, TaskUpdate)
- - `run_in_background` runs the command detached: it keeps running across turns and re-invokes you when it exits. No `&` needed.
- - Before assigning tasks to teammates, to see what's available
- - Include enough detail in the description for another agent to understand and complete the task - New tasks are created with status 'pending' and no owner - use TaskUpdate with the `owner` parameter to assign them
- - Working directory persists between calls, but prefer absolute paths \u2014 `cd` in a compound command can trigger a permission prompt. Shell state (env vars, functions) does not persist; the shell is initialized from the user's profile.
- - You can use the `run_in_background` parameter to run the command in the background. Only use this if you don't need the result immediately and are OK being notified when the command completes later. You do not need to check the output right away - you'll be notified when it finishes.
- --- name: ${...} description: TODO \u2014 describe WHEN Claude should use this. Include trigger phrases users might say ("do X", "set up Y", "review Z"). Be specific; this string is what Claude matches the user's request against. --- # ${...} TODO: what this skill does, and the steps Claude should take.
- --- name: ${...} description: TODO \u2014 one line shown in /output-style picker force-for-plugin: true keep-coding-instructions: true --- TODO: the style prompt. This is appended to Claude's system prompt while the style is active. With force-for-plugin: true, the style applies automatically when this plugin is enabled.
- --- name: example description: TODO \u2014 when should Claude delegate to this subagent? tools: - Read - Grep --- TODO: system prompt for the subagent.
- --author=${...}
- --auto-correct
- --available
- --cask
- --config <key=value>
- --cowork
- --cowork can only be used with user scope
- --diff-filter=M
- --disable-interactivity
- --exact
- --fix
- --id
- --keep-data
- --pretty=format:
- --prune
- --remote <name>
- --scope <scope>
- --shell=bun
- --sparse <paths...>
- --stdio
- --strict
- --write
- -a, --all
- -f, --force
- -i`, e.g. `git rebase -i`, `git add -i`) are not supported in this environment. - Use the `gh` CLI for GitHub operations (PRs, issues, API). - Commit or push only when the user asks. If on the default branch, branch first.${...}
- -jpeg
- -m, --message <msg>
- -s, --scope <scope>
- ... 6645 more
```

## Takeover Commands

```bash
node scripts/verify-upstream-compat.js --baseline 2.1.150 --skip-latest --native-macos-arm64 --json
node scripts/generate-upstream-text-diff.js --to 2.1.150 --native-macos-arm64
node scripts/promote-native-candidate.js --candidate <candidate-json>
```

## Done Criteria

- Add the missing source-of-truth translations or guard fix on this branch.
- Re-run the native latest candidate workflow for this version.
- Only promote support metadata after native verification and display audit pass.
- Remove or update this handoff report before marking the PR ready.
